# dependencies
/node_modules

# next.js
/.next/
/out/

# production
/build

# env
.env
.env.local
.env.*.local

# misc
.DS_Store
*.pem
npm-debug.log*

# typescript
*.tsbuildinfo
next-env.d.ts

# uploads (media stored locally in dev — see docker-compose volume in prod)
/storage/uploads/*
!/storage/uploads/.gitkeep

# testing
/coverage

# vercel
.vercel

# fichiers de message de commit temporaires
.git-commit-msg.txt

# documents générés (docs/ : PDF, etc.) — reproductibles via scripts/generate-*.ts, pas versionnés
docs/*.pdf
